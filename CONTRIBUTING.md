# 贡献指南

[English](CONTRIBUTING.en.md) | **简体中文**

感谢你愿意帮助改进 BEYOND 3.0。

本项目开放 Issues 和 Pull Requests（PR），但开放提交不等于自动采纳。每个 PR 都必须通过公开验证，并由维护者完成人工审查；即使测试全部通过，维护者仍可能因为产品方向、机制一致性、兼容性、维护成本或安全边界而不合并。

参与本项目即表示你同意遵守[社区行为准则](CODE_OF_CONDUCT.zh-CN.md)。

## 1. 怎样参与

- Bug、文档错误和可复现的行为偏差：提交 Bug Issue。
- 新能力、机制调整和较大范围重构：先提交功能建议 Issue，说明问题、目标和边界，再开始实现。
- 小型文档修正和明确缺陷：可以直接提交 PR，但仍需写清验证方式。
- 安全漏洞：不要公开披露复现细节、凭据或可利用信息，按[安全政策](SECURITY.md)报告。

提交前请先搜索已有 Issues 和 PR，避免重复工作。

## 2. 贡献边界

公开仓库只接受与下列内容直接相关的修改：

- 产品说明、快速开始、系统架构和公开示例；
- `模板交付包/`中的通用协同机制、文档模板和 Skills 源码；
- 公开仓库的验证脚本、Issue/PR 模板和持续集成配置。

请勿提交：

- 真实项目名称、业务数据、客户资料、服务器、账号、凭据或生产配置；
- 本机绝对路径、任务 ID、对话链接、运行日志和内部发布快照；
- 只对单一私有项目成立的固定阈值、流程或业务口径；
- 未获得再分发权的代码、文档、图片或其他材料。

## 3. 开发和验证

在仓库根目录运行公开内容检查：

```text
node scripts/check-public-content.mjs
```

在最小示例目录运行基线测试：

```text
npm test
npm run check
```

修改`模板交付包/skills/`时，还必须在 PR 中提供：

1. 真实问题、缺口或回归风险的修改前证据；已有直接证据时复用，不为流程制造失败测试。
2. 修改后与目标直接匹配的验证结果。
3. 至少一个受影响的相邻回归场景，证明没有破坏正常行为。
4. 对触发条件、控制权、授权、停止条件和兼容性的影响说明。

不要为了让测试通过而降低门禁、删除失败断言或把项目专用事实写进通用模板。

## 4. PR 必须包含什么

- 解决的问题与关联 Issue。
- 本次做什么、不做什么。
- 受影响的公开路径和兼容性判断。
- 实际运行的测试命令、结果和必要证据。
- 安全、授权、数据、Git 或生产边界是否变化。
- 如果仍有已知限制，必须明确列出。

一次 PR 尽量只解决一个完整问题。无关格式化、重命名和机制修改不要混在一起。

## 5. 合并门禁

PR 进入主分支前必须同时满足：

1. GitHub Actions 中的公开验证通过。
2. 所有审查意见已经处理或形成明确结论。
3. 至少一名有合并权限的维护者人工批准。
4. 不存在未说明的敏感信息、项目专用残留或授权扩张。
5. 维护者确认修改符合当前产品方向和长期维护边界。

测试通过只证明已声明的检查通过，不代表 PR 必然被采纳。维护者可以要求修改、暂缓、关闭或不合并，也不承诺 Issue 响应时间、PR 合并时间或功能交付时间。

公开仓库创建后，维护者还应在 GitHub 的`main`分支 ruleset 中启用“必须通过 PR”“至少 1 个批准”“必须通过公开验证状态检查”，并禁止绕过普通合并流程。仓库文件只能声明这项规则，真正的强制执行依赖 GitHub 仓库设置。

## 6. 贡献许可

本项目采用[Apache License 2.0](LICENSE)。除非你明确书面声明并与维护者达成其他协议，你有意提交并被项目接收的贡献将按 Apache License 2.0 提供，不附加额外条款。提交者必须确认自己有权提供该贡献。
