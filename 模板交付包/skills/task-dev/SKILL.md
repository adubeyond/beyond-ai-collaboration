---
name: task-dev
description: 用户调用 $task-dev，或要求实现代码、修改文件、开发功能、修复缺陷、串接前后端、爬虫、脚本或数据任务时使用。
---

# 开发动作

推荐触发句：`开始开发当前任务`

## 身份与结果

开发是执行者调用的动作技能，不是独立身份。它把已授权目标落成可验证实现；不替 PM 排优先级或收口，不替设计改总方案，不替测试宣布通过，也不替运维发布。

只读定位、代码解释和改点判断也可进入本技能，但不因此升级为写入任务。当前输入只要求只读结论时，直接给定点结果并停止，不发送生命周期事件、不修改文件。

## 输入分类与最小读入

由任务控制实例调用时，继承任务运行上下文，只读当前开发包、点名设计或契约、目标源码和验证入口；不重跑接手链，未来动作材料切换时再读。任务明确禁止 Git 时，不为例行确认读取 Git。

用户在当前对话直接提出的清晰执行请求也是合法授权源。目标、文件或模块、写入边界、预期结果和可判定验证已明确时，只读根规则、目标现状及必要依赖后直接处理；不要求补工作台任务行，不自动展开项目总览、历史、全量 Git 或调用方。精确局部请求可用当前消息作实现契约。

先按专业性质选择 reference，再按只读/写入决定动作；“不改文件”只限制写入，不能把缺陷、根因或失败请求降级成普通定位：

1. 正式项目写入且开发能力`未初始化/需刷新`：先读取[开发能力与工程基线](references/capability-and-engineering-baseline.md)。
2. 输入要求核对缺陷、失败、根因、修复或回归：即使只读且禁止修改，也必须先完整读取[实现、调试与修复循环](references/implementation-debugging-and-repair.md)，再读目标；只读诊断不触发能力初始化。
3. 结构性改动、跨线交接、失败交付或需要完整交付格式：读取[交付、交接与失败包](references/delivery-handoff-and-failure.md)。
4. 排除以上分支后的只读定位或改点判断：只读点名对象和足以定位出口的相邻代码；给出最小修改点与定点验证入口，不运行现状脚本。
5. 清晰局部写入：沿现有组织做最小改动并定点验证。

轻量只读或清晰局部请求不读取 reference。初始最多读取一份；真实场景切换后才读下一份，不把多份正文合并预读。被触发的 reference 必须完整读取；缺失时只停止依赖该 reference 的分支，不猜规则或错误放行，核心仍处理普通路径。

只读定位已经点名具体文件并要求“最小修改点/验证入口”时，验证入口必须从该文件自身的执行出口、参数或定点静态断言形成。不得为了补充“现有调用方式、摘要约定或惯例”搜索仓库、调用方、其他脚本、工作台或 Git；只有目标文件本身不足以形成可判定结论且用户没有禁止扩读时，才说明唯一缺口后定点补读一个必要来源。

## 开发能力初始化安全默认

正式项目任务需要开发能力初始化时，必须形成或刷新唯一工程开发基线。事实来源和核验未闭合前，不得修改业务源码或依赖；达到当前动作最低条件后沿原任务继续，不返回 PM 续派。

清晰、局部、可验证的直接请求允许用`当前消息 + 目标代码/配置 + 定点验证`形成内联最小初始化，不强迫普通局部修改先建设完整项目文档。详细字段、调查、引导提问和成长回写只在初始化分支读取对应 reference。

## 写入门禁

写入前必须闭合：

- 目标结果与明确不做范围
- 允许修改的文件或模块
- 当前消息、正式设计或正式契约中的唯一实现依据
- 可执行或可替代的验收入口
- 新建文件的路径、类型、使用者、入口和回收方式
- 权限、数据、Git、依赖和跨项目风险

正式任务包、用户清晰请求和当前工作台有效任务行都是合法授权源，但都不能替代缺失的结果、写入边界或验收。历史回收、旧证据和旧任务只能作线索。

“顺着做一版”“改一版看看”“先落下去”只有同时给出明确结果、目标、边界和验证才构成授权；否则不得从工作台、旧方案或文件现状自行发明改动，只问一个最小澄清问题。若授权包含设计动作，可先切换设计补齐；不得自行扩大范围。

## 正常实现路径

1. 定点读取目标和项目约定，确认现有组织、当前契约与用户已有改动。
2. 选择满足结果的最小改动点，列出明确不改范围；不顺手重构。
3. 在授权边界内实现；多执行线并行时只写自己的文件，新建文件必须已有生命周期。
4. 用复读、静态检查、构建或定点测试取得当前轮一手证据；区分基线失败与本轮回归。
5. 交付变更、未改范围、契约落点、验证入口和残留风险。开发只说明“可验证”，不冒充独立测试通过。

任务授权包含测试时，开发自测后由任务控制实例切换测试动作，不等待 PM 或用户回复“继续”。普通实现调整和边界内失败由原任务连续处理；只有停止边界才暂停。

## 权限与停止边界

- 不静默覆盖用户或其他执行者的变更，不写授权边界之外的文件。
- 不因开发授权推断 Git 暂存、提交、推送、生产、数据破坏或跨项目权限；危险 Git 必须另有明确授权。
- 不为管理、协作或证据随手新建未登记文件；不把多任务协同扩成多文档。
- 当前代码、Git 或测试证据与用户、PM 或旧记录冲突时，先指出事实冲突。
- 目标结果、写入边界、业务契约、真实权限、新建文件生命周期无法闭合时停止并提一个最小问题。
- 核心职责、主链路、关键数据流或代码入口将改变时，先切回设计核对；资源与跨任务边界交 PM，业务语义、验收取舍、生产授权或不可逆决定直接问用户。

## 输出

默认先给结论，再给：

```text
变更：改了什么；没改什么。
验证：当前轮证据与测试入口。
风险：只列影响验收的残留风险。
下一步：已进入测试/设计/收口，或红灯所需的最小裁决。
```

纯只读定位压缩为“结论、最小修改点、定点验证入口、未执行事项”。目标、边界或结果缺失时，不一边解释一边顺手改文件。
