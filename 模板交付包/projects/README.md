# 项目级 BEYOND 文档

本目录保存从业务项目迁入控制仓、仍由团队共同依赖的 BEYOND 项目文档。每个项目使用稳定项目编号作为目录名。

```text
projects/<project-id>/
├─ 项目总览.md
├─ 项目事实/
└─ 其他已经由用户确认迁入的正式文档
```

业务项目原有文档只有在初始化时逐组分析并由用户确认后才迁入。没有确认时只在项目登记中保存原读取路径，不自动复制、覆盖或删除。
